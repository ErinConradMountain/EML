# Terms

Purpose: hold shorter concept labels, provisional terminology, and terms that are not yet stable enough for full definitions.

Use this folder when:
- a term is recurring but still shifting
- a working study introduces a useful label that needs testing
- a concept should be tracked before it is formalized in `Definitions/`

When a term stabilizes, promote it into `../Definitions/`.
